The index purpose is when we have a tag that it has been linked to more than 20 or 30 notes, in this case there isn't any tag inside the folder that reach that number of notes.

If we want to create an index. first we write a new note inside the index folder, we give a title, then  inside the note we use the double  square brackets [[]] as sub-heading, to link our index to each note.  